/*let pedirNumero = prompt("por favor colocar un numero");
    alert("su numero es:" + pedirNumero);

    function suma(numero1, numero2){
        return = numero1 + nuemro2;
    }
*/
    //ciclo for
    for (numeros= 1; numeros <200; numeros++) {
        console.log(numeros);
    }
    // ciclo while

    var i = 10;
    while (i <10), i++; {
        alert(i);
    }